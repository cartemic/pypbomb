language: python
python:
- "3.8"

branches:
  only:
    - master
    - develop
    - ppf_sphinx

install:
    - pip install -r requirements.txt
    - pip install -r requirements_test.txt

script:
    - pytest -vv --cov=./;

after_success:
    - bash <(curl -s https://codecov.io/bash)
